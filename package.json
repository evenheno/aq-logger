{
    "name": "aq-logger",
    "version": "1.0.16",
    "description": "Aquarius Logger",
    "main": "index.js",
    "type": "commonjs",
    "scripts": {
        "build": "node scripts/pre-build && tsc && node scripts/post-build",
        "build:publish": "npm run build && cd dist && npm publish",
        "build:test": "npm run build && node dist/test",
        "start": "cd build && node .",
        "build:start": "npm run build && npm run start"
    },
    "author": "Aquarius Systems Inc.",
    "license": "ISC",
    "devDependencies": {
        "fs-extra": "^11.1.1",
        "typescript": "^4.9.5"
    },
    "dependencies": {
        "crc": "^4.3.2"
    }
}
